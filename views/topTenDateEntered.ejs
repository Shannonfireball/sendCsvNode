<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>CSV File</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="./style.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <ul class="nav nav-tabs">
      <li class="nav-item">
      <a href="http://localhost:9000/" class="nav-link"><button class="btn btn-outline-secondary">Home</button></a>
      </li>
      <li class="nav-item">
        <a href="http://localhost:9000/aggrigate" class="nav-link"><button class="btn btn-outline-secondary">Aggrigate</button></a>
      </li>
      <li class="nav-item">
        <a href="http://localhost:9000/toptendata" class="nav-link active"><button class="btn btn-outline-secondary">Top 10</button></a>
      </li>
    </ul>
    <form action="/toptendata" method="get" enctype="multipart/form-data">
      <div class="container mt-4">
      <div class="mb-3">
        <input type="text" class="form-control" name="date" placeholder="YYYY-MM-DD" />
      </div>
      <button type="submit" class="btn btn-success">Get Date</button>
      </div>
      <%if(data){%>
      <div class="container mt-4">
        <table class="table">
          <thead>
            <tr>
                <th>Date: <u><%= data[0].Date%></u></th>
            </tr>
            <tr>
              <th>Locations</th>
              <th>Cases</th>
            </tr>
          </thead>
          <tbody>
            <%for(var i=0; i < data.length; i++){%>
            <tr>
              <td><%= data[i].Town%></td>
              <td><%= data[i].Count%></td>
            </tr>
            <%}%>
          </tbody>
        </table>
      </div>
      <%}%>
    </form>
  </body>
</html>